// The home hub renders the browse view from the manifest; prerender it to real,
// crawlable HTML (overriding the root layout's client-only defaults).
export const prerender = true;
export const ssr = true;
