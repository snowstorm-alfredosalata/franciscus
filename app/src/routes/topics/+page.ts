// Topic index rendered from the manifest; prerender to crawlable HTML. The
// topic-detail route one level down stays client-only (it needs the DB).
export const prerender = true;
export const ssr = true;
